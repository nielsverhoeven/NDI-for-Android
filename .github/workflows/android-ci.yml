name: Android CI (disabled)
# This workflow has been superseded by maui-ci.yml following the migration to .NET MAUI.
# It is kept here as a historical reference only and will never trigger.
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled. Use maui-ci.yml instead.'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Android CI is disabled. The project has migrated to .NET MAUI. See maui-ci.yml."
