name: e2e-dual-emulator (disabled)
# Superseded by maui-ci.yml. The testing/e2e and ndi/sdk-bridge paths no longer exist.
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Disabled — paths testing/e2e and ndi/sdk-bridge removed in #133.'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "e2e-dual-emulator is disabled. See maui-ci.yml."
