name: E2E Matrix Nightly (disabled)
# Superseded by maui-ci.yml. The testing/e2e paths no longer exist.
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Disabled — testing/e2e removed in #133.'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "E2E Matrix Nightly is disabled. See maui-ci.yml."
