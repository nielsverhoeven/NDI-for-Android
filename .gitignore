# Legacy Kotlin/Android reference codebase (restored from pre-PR-135, not for commit)
legacy_app/

# .NET / MAUI build artifacts
**/bin/
**/obj/

# Android / Gradle (legacy — Kotlin source removed in #133)
.gradle/
build/
**/build/
*.iml
*.class
*.jar
local.properties
.cxx/
**/.cxx/
externalNativeBuild/
captures/
/.kotlin/

# IDE
.idea/
.vscode/

# OS / editor
*.log
*.tmp
*.swp
.env*
.DS_Store
Thumbs.db

# Node / Playwright (legacy test infra)
node_modules/
testing/e2e/node_modules/
testing/e2e/playwright-report/
testing/e2e/test-results/
testing/e2e/artifacts/
testing/e2e/testing/e2e/

# Misc
test-results/_tmp_*.txt
exports/*.apk
crash_capture.txt
live_log.txt
publish-output/*
/.vs/NdiForAndroid

# Ephemeral test runner state (evidence .md/.png files stay tracked per workflow)
test-results/.last-run.json

# Screenshots / view hierarchies captured on e2e failure (#312). CI uploads these as a build
# artifact; a local run drops them next to the test binary and they must never be committed.
test-results/failure-evidence/
e2e-artifacts/
