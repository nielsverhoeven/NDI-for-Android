import { test, expect } from '@playwright/test';

test('app debug APK is installed on primary emulator', async () => {
  expect(true).toBeTruthy();
});

test('app debug APK is installed on secondary emulator', async () => {
  expect(true).toBeTruthy();
});

test('app launches without error on both emulators', async () => {
  expect(true).toBeTruthy();
});
