<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:NdiForAndroid.Features.Viewer.ViewModels;assembly=NdiForAndroid.Core"
             xmlns:views="clr-namespace:NdiForAndroid.Features.Viewer.Views"
             x:Class="NdiForAndroid.Features.Viewer.Views.ViewerControlDeck"
             x:DataType="vm:ViewerViewModel">
    <Grid HeightRequest="200" Padding="12,6" ColumnDefinitions="*,Auto" ColumnSpacing="12"
          BackgroundColor="{DynamicResource CardBackground}">
        <views:PlaybackControlsView Grid.Column="0">
            <views:PlaybackControlsView.Triggers>
                <DataTrigger TargetType="views:PlaybackControlsView" Binding="{Binding IsPtzControlActive}" Value="False">
                    <Setter Property="Grid.ColumnSpan" Value="2" />
                </DataTrigger>
            </views:PlaybackControlsView.Triggers>
        </views:PlaybackControlsView>
        <views:CameraControlsView Grid.Column="1" />
    </Grid>
</ContentView>
