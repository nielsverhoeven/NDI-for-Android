<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:NdiForAndroid.Features.Home.ViewModels;assembly=NdiForAndroid.Core"
             xmlns:t="clr-namespace:NdiForAndroid.Testing;assembly=NdiForAndroid.Core"
             x:Class="NdiForAndroid.Features.Home.Views.HomePage"
             x:DataType="vm:HomeViewModel"
             AutomationId="{x:Static t:TestIds.HomePage}"
             Title="Home"
             BackgroundColor="{DynamicResource PageBackground}">
    <!-- Status-card label colour by state (#370 home-nav-08): Idle=TextSecondary (base setter),
         Active=SuccessText, Failure=ErrorText. Shared style so the Discovery card uses the exact
         same rule as Viewer/Output below; the inline style must restate FontFamily/FontSize since
         it replaces the implicit Label style from Styles.xaml. -->
    <ContentPage.Resources>
        <Style x:Key="HomeStatusLabelStyle" TargetType="Label">
            <Setter Property="FontFamily" Value="OpenSansRegular" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="TextColor" Value="{DynamicResource TextSecondary}" />
            <Style.Triggers>
                <DataTrigger TargetType="Label" Binding="{Binding DiscoveryStatusKind}" Value="Active">
                    <Setter Property="TextColor" Value="{DynamicResource SuccessText}" />
                </DataTrigger>
                <DataTrigger TargetType="Label" Binding="{Binding DiscoveryStatusKind}" Value="Failure">
                    <Setter Property="TextColor" Value="{DynamicResource ErrorText}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </ContentPage.Resources>
    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">
            <!-- Discovery Status Card -->
            <Frame AutomationId="{x:Static t:TestIds.HomeDiscoveryStatusCard}" BackgroundColor="{DynamicResource CardBackground}" BorderColor="{DynamicResource BorderColor}" HasShadow="True" CornerRadius="12" Padding="16">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Discovery Status" SemanticProperties.HeadingLevel="Level2" FontAttributes="Bold" FontSize="18" TextColor="{DynamicResource TextPrimary}"/>
                    <Label AutomationId="{x:Static t:TestIds.HomeDiscoveryStatus}" Text="{Binding DiscoveryStatus}" Style="{StaticResource HomeStatusLabelStyle}"/>
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                        <Label Grid.Column="0" AutomationId="{x:Static t:TestIds.HomeSourceCount}" Text="{Binding SourceCount, StringFormat='Sources found: {0}'}" FontSize="14" TextColor="{DynamicResource TextSecondary}"/>
                        <Label Grid.Column="1" AutomationId="{x:Static t:TestIds.HomeLastRefresh}" Text="{Binding LastRefreshDisplay}" FontSize="14" TextColor="{DynamicResource TextSecondary}" HorizontalOptions="End"/>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Viewer Status Card -->
            <Frame AutomationId="{x:Static t:TestIds.HomeViewerStatusCard}" BackgroundColor="{DynamicResource CardBackground}" BorderColor="{DynamicResource BorderColor}" HasShadow="True" CornerRadius="12" Padding="16">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Viewer Status" SemanticProperties.HeadingLevel="Level2" FontAttributes="Bold" FontSize="18" TextColor="{DynamicResource TextPrimary}"/>
                    <Label AutomationId="{x:Static t:TestIds.HomeViewerStatus}" Text="{Binding ViewerStatus}" FontSize="15">
                        <Label.Style>
                            <Style TargetType="Label">
                                <Setter Property="FontFamily" Value="OpenSansRegular" />
                                <Setter Property="TextColor" Value="{DynamicResource TextSecondary}" />
                                <Style.Triggers>
                                    <DataTrigger TargetType="Label" Binding="{Binding ViewerStatusKind}" Value="Active">
                                        <Setter Property="TextColor" Value="{DynamicResource SuccessText}" />
                                    </DataTrigger>
                                    <DataTrigger TargetType="Label" Binding="{Binding ViewerStatusKind}" Value="Failure">
                                        <Setter Property="TextColor" Value="{DynamicResource ErrorText}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Label.Style>
                    </Label>
                </VerticalStackLayout>
            </Frame>

            <!-- Output Status Card -->
            <Frame AutomationId="{x:Static t:TestIds.HomeOutputStatusCard}" BackgroundColor="{DynamicResource CardBackground}" BorderColor="{DynamicResource BorderColor}" HasShadow="True" CornerRadius="12" Padding="16">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Output Status" SemanticProperties.HeadingLevel="Level2" FontAttributes="Bold" FontSize="18" TextColor="{DynamicResource TextPrimary}"/>
                    <Label AutomationId="{x:Static t:TestIds.HomeOutputStatus}" Text="{Binding OutputStatus}" FontSize="15">
                        <Label.Style>
                            <Style TargetType="Label">
                                <Setter Property="FontFamily" Value="OpenSansRegular" />
                                <Setter Property="TextColor" Value="{DynamicResource TextSecondary}" />
                                <Style.Triggers>
                                    <DataTrigger TargetType="Label" Binding="{Binding OutputStatusKind}" Value="Active">
                                        <Setter Property="TextColor" Value="{DynamicResource SuccessText}" />
                                    </DataTrigger>
                                    <DataTrigger TargetType="Label" Binding="{Binding OutputStatusKind}" Value="Failure">
                                        <Setter Property="TextColor" Value="{DynamicResource ErrorText}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Label.Style>
                    </Label>
                </VerticalStackLayout>
            </Frame>

            <!-- Quick Actions -->
            <Label Text="Quick Actions" SemanticProperties.HeadingLevel="Level2" FontAttributes="Bold" FontSize="18" TextColor="{DynamicResource TextPrimary}" Margin="0,8,0,0"/>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Button Text="Start Viewing Last Source"
                        AutomationId="{x:Static t:TestIds.HomeStartViewingLast}"
                        Command="{Binding StartViewingLastSourceCommand}"
                        IsEnabled="{Binding HasLastViewerSource}"
                        BackgroundColor="{DynamicResource Primary}"
                        TextColor="{DynamicResource TextOnAccent}"
                        CornerRadius="10"
                        MinimumHeightRequest="48"
                        FontAttributes="Bold"/>

                <Button Grid.Column="1"
                        Text="Resume Output"
                        AutomationId="{x:Static t:TestIds.HomeResumeOutput}"
                        Command="{Binding ResumeOutputCommand}"
                        IsEnabled="{Binding CanResumeOutput}"
                        BackgroundColor="{DynamicResource SuccessFill}"
                        TextColor="{DynamicResource TextOnAccent}"
                        CornerRadius="10"
                        MinimumHeightRequest="48"
                        FontAttributes="Bold"/>
            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
